# ── Dependencies & build output ──────────────────────────────────────────────
node_modules
server/node_modules
dist
.vercel

# ── Secrets / local env ──────────────────────────────────────────────────────
.env
.env.local
server/.env
gps-sender/.env

# ── Editor / AI tooling — keep OUT of the repo (no AI traces) ─────────────────
.claude/
.cursor/
.idea/
.vscode/

# ── Proposal & paper artifacts — NOT part of the product, do not publish ──────
*.docx
*.pdf
FleetTrack_*.md
Herry_*.md
conference-template*.docx
fig*.png
# paper/figure build scripts (research tooling, not the product)
scripts/build_paper.py
scripts/make_fig1.py
scripts/make_figs.py

# ── Python (gps-sender) ──────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── OS noise ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
