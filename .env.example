# Dashboard → cloud server connection.
# Copy to `.env` (or `.env.local`) and point it at your FleetTrack server.
# Local dev default:
VITE_API_URL=http://localhost:4000
# LAN example (so phones/other PCs on the same Wi-Fi can load the dashboard):
# VITE_API_URL=http://192.168.1.10:4000
